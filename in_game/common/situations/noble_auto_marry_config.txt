noble_auto_marry_config = {
	#menu for manually triggering events
	monthly_spawn_chance = monthly_spawn_chance_unique
	hint_tag = hint_noble_auto_marry
	
	can_start = {
		always = yes
	}
	
	can_end = {
		has_variable = noble_auto_marry_i_have_made_my_bed_and_will_lay_in_it
	}
	
	visible = {
		is_ai = no
	}

	on_start = {
		scope:actor ?= {
			trigger_event_non_silently = {
				id = noble_auto_marry.63 # entry config menu
			}
		}
	}
}
